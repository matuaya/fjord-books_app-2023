<nav>
  <ul>
    <li><%= button_to t('views.generals.logout'), destroy_user_session_path, method: :delete %></li>
    <li><%= link_to t('views.generals.edit_account'), edit_user_registration_path %></li>
    <li><%= link_to t('views.generals.show_users_list'), users_path %></li>
  </ul>
</nav>
<h1>Books</h1>

<div id="books">
  <% @books.each do |book| %>
    <%= render book %>
    <p>
      <%= link_to t('views.generals.show_this_book'), book %>
    </p>
  <% end %>
</div>

<%= link_to t('views.generals.new_book'), new_book_path %>
