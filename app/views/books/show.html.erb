<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to t('views.actions.edit_this_book'), edit_book_path(@book) %> |
  <%= link_to t('views.actions.back_to_books'), books_path %>

  <%= button_to t('views.actions.destroy_this_book'), @book, method: :delete %>
</div>
